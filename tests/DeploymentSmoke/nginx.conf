events { }
http {
    server {
        listen 80;
        location /operations/ {
            proxy_pass http://host.docker.internal:__PORT__;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
