# Acta certification seal

**Verdict: PASS** — the 0.9.0 standard run on SQL Server, second gate of the re-cut round on the
release commit: provider parity for the PostgreSQL run that passed thirteen minutes earlier, same
shape, same counts, on an engine with different locking, isolation, and routine dialect.

## Run

| | |
|---|---|
| Captured | 2026-08-16T08:32:31Z |
| Commit | `8f03356` — the release commit |
| Provider | SQL Server 2022 (RTM-CU26, 16.0.4265) |
| Schema | `cert09msc` |
| Run id | `r20260816-081916-672cb8` |
| Workload | Anvil `CrashRecovery`, **10,000 jobs**: 9,000 `slow-success` (5 durable steps at 1000ms) and 1,000 `at-most-once-charge`, plus **5,000 outbox rows** staged into the producer database across the chaos window and relayed by `sys.outbox` |
| Workers | 8 processes × 8 executors = **64 concurrent slots**, 80 registrations across the run as kills were respawned |
| Chaos | Real `Process.Kill` every 5s for a 7-minute window |
| Quiesce | Chaos stopped, then the staging file drained to empty, then every job carrying this run's correlation key reached a terminal status |
| Wall clock | 13m 15s from start to verdict |

**Timing — framework production defaults, unmodified**: `HeartbeatInterval` 45s, with
`LeaseTtlSeconds` 180 and `WorkerDeadAfter` 315s derived from it at x4 and x7.

## Result

```
  [ok  ] outbox-drained               pending+claimed=0 quarantined=0
  [ok  ] outbox-delivered             staged=5000 ledger_jobs=5000

  ACTA CERTIFICATION  |  mssql  |  cert09msc

  [ok  ] exec-event-ownership         0
  [ok  ] attempt-pairing              0
  [ok  ] namespace-isolation          0
  [ok  ] no-stranded-work             0
  [ok  ] expected-outcome             0
  [ok  ] terminal-integrity           0
  [ok  ] step-replay                  0
  [ok  ] chaos-was-real               orphaned_attempts=574 workers_marked_dead=72
  [note] clock-backsteps              backwards_writes=89
  [ok  ] at-most-once                 0
  [ok  ] outbox-relayed-outcome       0
  [note] outbox-relayed               relayed_jobs=5000
  [ok  ] tenant-context               0

  PASS - every asserted property held and the chaos was real.
```

Why the whole round was re-run on `8f03356` is recorded in the PostgreSQL seal of the same morning
(`seal-20260816T081858Z.md`); the outbox split-check design is described in the PostgreSQL seal of
the previous round (`seal-20260815T020214Z.md`). This run reproduces both on the second engine.

## What the ledger holds afterwards

| | |
|---|---|
| Jobs seeded | 15,000 — 10,000 workload + 5,000 relayed outbox receipts (plus 5 recurring system slots) |
| Reached `Succeeded` | **15,000** — every one, charges and receipts included |
| Durable step rows | 46,000 |
| Events appended | 88,441 |
| Worker registrations | 80 (64 slots live at any moment; the rest are respawns) |
| Workers reclaimed dead | 72 |
| Attempts orphaned by a kill | 574 |
| Tenant witness notes | 10,573 |

**The two engines converged, again.** 574 orphaned attempts against PostgreSQL's 579, 72 workers
reclaimed dead against 73, identical job, step, and receipt totals to the row — and 574/72 is the
same pair this engine produced on the previous round's seal, which is the second convergence worth
noting: the re-cut moved nothing. The clock-backsteps note diverges the other way as it always
does — 89 here against 13,885 on PostgreSQL — which is the standing argument for keeping it
measured, never asserted: it tracks lock-wait patterns, not correctness, and every asserted
property passed on both.

**Stated plainly:** zero charges were interrupted mid-body this run (as on the PostgreSQL run), so
`at-most-once = 0` is vacuous here; the non-vacuous version is this round's ensemble seal
(`seal-20260816T090216Z.md`).

## Scope

One namespace, one machine, one participant; `namespace-isolation` cannot fail with a single
namespace in the schema — the 3-participant / 2-namespace ensemble seal covers that. The checks are
`anvil/Anvil/certify.sql` with the schema token bound to `cert09msc`.
