# Acta certification seal

**Verdict: PASS** — the `1.0.0-rc.3` SQL Server standard gate, second of the round's four. The
same shape as the PostgreSQL gate: one process, 10,000 jobs, 64 executor slots across eight
workers, a real kill every five seconds for seven minutes.

## Run

| | |
|---|---|
| Captured | 2026-09-21T23:55:27Z |
| Commit | `d3def975` (the certified code commit; the harness ran that tree less one conformance test file) |
| Provider | SQL Server 2022 (Linux container) |
| Schema | `certrc3fms` |
| Run id | `r20260921-234152-957096` |
| Workload | **10,000 jobs** plus **5,000 outbox rows** staged under chaos; 1,000 of the jobs are the `metered` shape at `600/m` |
| Workers | 8 processes × 8 executors, **80 registrations** across the run |
| Chaos | Real `Process.Kill` every 5s for a 7-minute window |
| Wall clock | 13m 35s from seed to verdict |

## Result

| Check | Result |
|---|---|
| `outbox-drained` | pending+claimed=0 quarantined=0 |
| `outbox-delivered` | staged=5000 ledger_jobs=5000 |
| `exec-event-ownership` | 0 |
| `attempt-pairing` | 0 |
| `namespace-isolation` | 0 |
| `no-stranded-work` | 0 |
| `expected-outcome` | 0 |
| `terminal-integrity` | 0 |
| `step-replay` | 0 |
| `chaos-was-real` | orphaned_attempts=574, workers_marked_dead=72 |
| `clock-backsteps` *(note)* | backwards_writes=478 |
| `at-most-once` | 0 |
| `outbox-relayed-outcome` | 0 |
| `outbox-relayed` *(note)* | relayed_jobs=5000 |
| `tenant-context` | 0 |
| `rate-contract` *(note)* | rate=600/m admitted=1000 max_1s=21 budget_1s=30 max_10s=111 budget_10s=120 |

## What the ledger holds afterwards

| | |
|---|---|
| `jobs` | 15,005 — 15,000 seeded plus the five recurring system slots |
| Terminal `Succeeded` | 15,000 |
| Terminal `Failed` | 0 |
| `Ready` at rest | 5 — the system slots |
| Durable step rows | 41,000 |
| Events | 88,324 |
| Worker registrations | 80 |
| Alerts | 574 |

## Scope

One process, one namespace, one SQL Server schema. Checks are `anvil/Anvil/certify.sql` bound to
`certrc3fms` plus the `rate-contract` window scan in `CertifyVerdict`; re-run `anvil certify`
against that schema to reproduce the verdict.
