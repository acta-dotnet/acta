# Acta certification seal

**Verdict: PASS** — the `1.0.0-rc.3` SQLite gate, third of the round's four, at the reduced shape
the single-writer provider gets: six workers of eight executors, 48 slots, on one WAL file, 10,000
jobs, a real kill every five seconds for seven minutes.

## Run

| | |
|---|---|
| Captured | 2026-09-22T00:12:41Z |
| Commit | `d3def975` (the certified code commit; the harness ran that tree less one conformance test file) |
| Provider | SQLite, embedded, one WAL file |
| Schema | `certrc3fsq` |
| Run id | `r20260921-235527-a067d8` |
| Workload | **10,000 jobs** plus **5,000 outbox rows** staged under chaos; 1,000 of the jobs are the `metered` shape at `600/m` |
| Workers | 6 processes × 8 executors, **78 registrations** across the run |
| Chaos | Real `Process.Kill` every 5s for a 7-minute window |
| Wall clock | 17m 14s from seed to verdict |

## Result

| Check | Result |
|---|---|
| `outbox-drained` | pending+claimed=0 quarantined=0 |
| `outbox-delivered` | staged=5000 ledger_jobs=5000 |
| `exec-event-ownership` | 0 |
| `attempt-pairing` | 0 |
| `namespace-isolation` | 0 |
| `no-stranded-work` | 0 |
| `expected-outcome` | 0 |
| `terminal-integrity` | 0 |
| `step-replay` | 0 |
| `chaos-was-real` | orphaned_attempts=576, workers_marked_dead=72 |
| `clock-backsteps` *(note)* | backwards_writes=0 |
| `at-most-once` | 0 |
| `outbox-relayed-outcome` | 0 |
| `outbox-relayed` *(note)* | relayed_jobs=5000 |
| `tenant-context` | 0 |
| `rate-contract` *(note)* | rate=600/m admitted=1000 max_1s=20 budget_1s=30 max_10s=109 budget_10s=120 |

## What the ledger holds afterwards

| | |
|---|---|
| `jobs` | 15,005 — 15,000 seeded plus the five recurring system slots |
| Terminal `Succeeded` | 15,000 |
| Terminal `Failed` | 0 |
| `Ready` at rest | 5 — the system slots |
| Durable step rows | 41,000 |
| Events | 88,928 |
| Worker registrations | 78 |
| Alerts | 577 |

## Scope

One machine, one namespace, one SQLite file. SQLite is single-node, so multi-process namespace
isolation is out of scope here and carried by the ensemble seal; what this gate proves is that the
single-writer provider holds every other property at 48 slots under the same kill cadence. Checks
are `anvil/Anvil/certify.sql` bound to `certrc3fsq` plus the `rate-contract` window scan in
`CertifyVerdict`; re-run `anvil certify` against that file to reproduce the verdict.
