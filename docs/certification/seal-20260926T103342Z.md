# Acta certification seal

**Verdict: PASS** — the `1.0.0` PostgreSQL standard gate, first of the round's four. One process,
10,000 jobs, 64 executor slots across eight workers, a real kill every five seconds for seven
minutes, then the ledger judged by `certify.sql`. The harness ran as a Native AOT executable.

## Run

| | |
|---|---|
| Captured | 2026-09-26T10:33:42Z |
| Commit | `984eb036` (the certified code commit) |
| Provider | PostgreSQL 18.4 (Debian 18.4-1.pgdg13+1, x86_64) |
| Schema | `certv1dpg` |
| Run id | `r20260926-102028-f4c2ad` |
| Workload | **10,000 jobs** plus **5,000 outbox rows** staged under chaos; 1,000 of the jobs are the `metered` shape at `600/m` |
| Workers | 8 processes × 8 executors, **80 registrations** across the run |
| Chaos | Real `Process.Kill` every 5s for a 7-minute window |
| Wall clock | 13m 14s from seed to verdict |

## Result

| Check | Result |
|---|---|
| `outbox-drained` | pending+claimed=0 quarantined=0 |
| `outbox-delivered` | staged=5000 ledger_jobs=5000 |
| `exec-event-ownership` | 0 |
| `attempt-pairing` | 0 |
| `namespace-isolation` | 0 |
| `no-stranded-work` | 0 |
| `expected-outcome` | 0 |
| `terminal-integrity` | 0 |
| `step-replay` | 0 |
| `chaos-was-real` | orphaned_attempts=576, workers_marked_dead=72 |
| `clock-backsteps` *(note)* | backwards_writes=13549 |
| `at-most-once` | 0 |
| `outbox-relayed-outcome` | 0 |
| `outbox-relayed` *(note)* | relayed_jobs=5000 |
| `tenant-context` | 0 |
| `rate-contract` *(note)* | rate=600/m admitted=1000 max_1s=19 budget_1s=30 max_10s=110 budget_10s=120 |

The rate meter admitted the 1,000 metered jobs at the declared ten per second under the kills: the
busiest second held 19 admissions against a budget of 30, the busiest ten seconds 110 against 120.
This is the first gate run on the tree whose meter reads its clock after the bucket row's lock, and
whose start and completion writes repeat and reconcile against the row, whose heartbeat returns a
claim with a lost answer to Ready, and whose executor and orphan release take a per-execution owner
entry before touching a row; 576 orphaned attempts across 72 dead workers found no attempt started
twice and no work stranded.

## What the ledger holds afterwards

| | |
|---|---|
| `jobs` | 15,005 — 15,000 seeded plus the five recurring system slots |
| Terminal `Succeeded` | 15,000 |
| Terminal `Failed` | 0 |
| `Ready` at rest | 5 — the system slots |
| Durable step rows | 41,000 |
| Events | 88,325 |
| Worker registrations | 80 |
| Alerts | 576 |

## Scope

One process, one namespace, one PostgreSQL schema. Checks are `anvil/Anvil/certify.sql` bound to
`certv1dpg` plus the `rate-contract` window scan in `CertifyVerdict`; re-run `anvil certify`
against that schema to reproduce the verdict.
